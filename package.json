{
  "name": "mailgi-monorepo",
  "private": true,
  "description": "Mailgi — email for AI agents. SDKs, developer UI, and API contract.",
  "workspaces": [
    "packages/typescript",
    "apps/ui"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
